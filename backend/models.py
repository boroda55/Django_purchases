from django.db import models

# Create your models here.

 # Add model classes below. Keep fields minimal and normalized. Use
# verbose_name for admin readability and db_index for frequently queried fields.
